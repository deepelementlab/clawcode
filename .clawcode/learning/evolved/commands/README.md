# `.clawcode/learning/evolved/commands/`

Evolved **slash command** definitions (markdown), produced when the learning pipeline promotes a reusable command.

May be merged or copied into `.clawcode/commands/` for TUI registration.
