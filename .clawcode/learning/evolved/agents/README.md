# `.clawcode/learning/evolved/agents/`

Evolved **subagent role** markdown (frontmatter + body), produced when the pipeline promotes a reusable agent definition.

May be copied into `.claw/agents/`, `.clawcode/agents/`, or `.claude/agents/` per your merge strategy.
