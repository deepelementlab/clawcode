# `.clawcode/learning/instincts/personal/`

Markdown files containing **instincts learned** in this workspace (generated by learning flows, e.g. consolidation from `observations.jsonl`).

Filenames often follow `learned-<timestamp>.md`.

## Git

Optional commit: share only non-sensitive, team-approved instinct packs.
