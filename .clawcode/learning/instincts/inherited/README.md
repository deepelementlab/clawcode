# `.clawcode/learning/instincts/inherited/`

**Imported** instinct bundles (from `/experience` import flows or manual drops). Filenames often like `import-<timestamp>.md`.

Use this directory to merge **org-wide** or **upstream** instincts without mixing them with freshly learned personal files.

## Git

Commit when you vendor a **blessed** inherited pack for the team.
