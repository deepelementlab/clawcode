# `.clawcode/learning/team-experience/exports/`

**Exported** team-experience bundles for sharing outside the workspace or for archival before import into another clone.

## Git

Treat exports as **sensitive** if they embed project context; use `.gitignore` by default.
