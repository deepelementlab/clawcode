# `.clawcode/claw_metrics/snapshots/`

Optional **point-in-time snapshots** of metric state (JSON), used by tuning and canary flows alongside `events.jsonl` and `canary_results.jsonl`.

Created on demand; may be absent in a fresh checkout.

## Git

Usually **ignored**; large or ephemeral.
