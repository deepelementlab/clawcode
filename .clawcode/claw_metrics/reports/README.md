# `.clawcode/claw_metrics/reports/`

Optional subdirectory for **timestamped metric reports** (e.g. `*-backend.json` / `*.md`) emitted by tuning or observability runs.

Not every workspace has this folder until a report-generating flow creates it.

## Git

Treat as **local or CI output** unless you intentionally snapshot a baseline.
