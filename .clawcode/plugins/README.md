# `.clawcode/plugins/`

Optional **plugin packages** or layouts discovered by the plugin loader together with:

- `.claw/plugins/`
- `.claude/plugins/`

See `plugin/loader.py` for search order and packaging expectations.

## Git

Commit **project plugins** you ship with the repo; keep experimental plugins local or behind branches.
